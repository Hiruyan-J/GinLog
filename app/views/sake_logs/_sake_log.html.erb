<div id="<%= dom_id sake_log %>">
  <div class="my-5">
    <p class="font-medium mb-1"><%= "product_name:#{sake_log.sake.product_name}" %></p>
    <p class="font-medium mb-1"><%= "Rating:#{sake_log.rating}" %></p>
  </div>
</div>