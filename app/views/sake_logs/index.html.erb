<% content_for :title,  t('.title') %>

<div class="w-full">
  <h1 class="font-bold text-4xl"><%= t('.title') %></h1>
  <div id="sake_logs" class="flex flex-col gap-2">
    <% if @sake_logs.any? %>
      <%= render @sake_logs %>
    <% else %>
      <p class="text-center my-10">まだ記録がありません</p>
    <% end %>
  </div>
</div>
